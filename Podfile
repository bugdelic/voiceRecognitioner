# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'PaperCompany' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PaperCompany

  # Pods for SpeakToMe
  pod 'OSC'
  pod 'SwiftOSC', '~> 1.1'
  pod 'Realm'
  pod 'FMDB'
  pod 'MagicalRecord'
  pod 'BlueCapKit', '~> 0.6'

  target 'PaperCompanyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PaperCompanyUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
